@model IEnumerable<Employees.Data.Models.Employee>

@{
    ViewBag.Title = "Index";
    ViewBag.Message = "Hello !";
}

<h2>@ViewBag.Message</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@Scripts.Render("~/Content/JavaScript/js.js")


@using (Html.BeginForm())
{
    <div class="form-group has-search">
        <input type="text" name="SearchString" id="search" placeholder="search.." onkeypress="myFunction()" />
    </div>

}


<p>
    @Html.ActionLink("gmail", "Find")
</p>

@foreach (var item in Model)
{
    @Html.Partial("_Employee", item)
}

<p onmouseover="display()" > Hover over me</p>


<p></p>
<p id="demo"></p>


<div class="tooltip">
    Hover over me
    <span class="tooltiptext">Tooltip text</span>
</div>

<div>Hover over me</div>

<span class="d-inline-block" tabindex="0" data-toggle="tooltip" title="Disabled tooltip">
    <button class="btn btn-primary" style="pointer-events: none;" type="button" disabled>Disabled button</button>


</span>